const addUserQuery = require('./addUser');
const checkUserQuery = require('./checkUserExistance');
const loginQuery = require('./login');
const addUserImageQuery = require('./addUserImage');

module.exports = {
  addUserQuery, checkUserQuery, loginQuery, addUserImageQuery,
};
