const { addUserQuery, checkUserQuery, loginQuery } = require('./users');
const { createPostQuery } = require('./posts');

module.exports = { addUserQuery, checkUserQuery, loginQuery, createPostQuery };
