const getPosts = require('./getPosts');
const createPost = require('./createPosts');
const getUserPosts = require('./getUserPosts');
const deletePost = require('./deletePost');

module.exports = {
  getPosts, createPost, getUserPosts, deletePost,
};
